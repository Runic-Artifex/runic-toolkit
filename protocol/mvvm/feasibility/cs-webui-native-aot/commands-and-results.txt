# Raw command and result evidence

All mutable work happened outside the repository at:

```text
C:\Users\vja\AppData\Local\Temp\webuitoolkit-cs-webui-aot-20260722-main
```

The path is included for auditability of this run only. Reproduction should use a new temporary directory and the immutable SHAs below.

## 1. Pin and clone the actual wrapper

```powershell
git ls-remote https://github.com/ViktorJannicke/cs-webui.git HEAD
git clone --no-tags https://github.com/ViktorJannicke/cs-webui.git "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\cs-webui"
git -C "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\cs-webui" rev-parse HEAD
```

Key output:

```text
f706387ae8ac62fe1ad2cfc011dc088751c8f556  HEAD
f706387ae8ac62fe1ad2cfc011dc088751c8f556
```

Commit metadata:

```text
2026-07-16T16:43:02+02:00
Merge pull request #1 from ViktorJannicke/codex/static-msvc-runtime
```

## 2. Restore, build, and managed-only test baseline

Run from the wrapper checkout:

```powershell
dotnet restore CsWebUi.sln --locked-mode
dotnet build CsWebUi.sln --configuration Release --no-restore
dotnet test CsWebUi.sln --configuration Release --no-build --logger "console;verbosity=normal"
```

Selected output:

```text
Build succeeded.
    0 Warning(s)
    0 Error(s)

Total tests: 22
     Passed: 19
    Skipped: 3
```

Each skip said:

```text
CSWEBUI_NATIVE_LIBRARY is not configured; native smoke tests require a built WebUI library.
```

This run is recorded as a managed baseline, not native proof.

## 3. Build the wrapper's pinned WebUI native revision

Read `nodes.webui.locked.rev` from the wrapper's `flake.lock`, then:

```powershell
git clone --no-tags https://github.com/webui-dev/webui.git "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-source"
git -C "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-source" checkout --detach 6c561ed739ce415e1e48ed17cb67c880aff1dc9d

& 'C:\Program Files\Microsoft Visual Studio\18\Enterprise\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe' `
  -S "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-source" `
  -B "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-build" `
  -G 'Visual Studio 18 2026' -A x64 `
  -DBUILD_SHARED_LIBS=ON `
  -DWEBUI_BUILD_EXAMPLES=OFF `
  -DWEBUI_OUT_LIB_NAME=webui-2 `
  -DWEBUI_USE_TLS=OFF `
  -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded

& 'C:\Program Files\Microsoft Visual Studio\18\Enterprise\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe' `
  --build "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-build" --config Release --parallel

& 'C:\Program Files\Microsoft Visual Studio\18\Enterprise\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe' `
  --install "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-build" `
  --prefix "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-install" --config Release
```

Selected output:

```text
The C compiler identification is MSVC 19.51.36248.0
The CXX compiler identification is MSVC 19.51.36248.0
webui.vcxproj -> ...\Release\webui-2.dll
Installing: ...\webui-native-install/bin/webui-2.dll
configure=0 build=0 install=0
```

Validate the binding surface against the exact pinned header:

```powershell
& 'C:\Program Files\Git\bin\bash.exe' `
  "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\cs-webui\eng\validate-webui-abi.sh" `
  "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-source\include\webui.h"
```

Output:

```text
Validated 111 WebUI exports.
exit 0
```

`dumpbin /DEPENDENTS webui-2.dll` reported:

```text
WS2_32.dll
USER32.dll
SHELL32.dll
ole32.dll
KERNEL32.dll
ADVAPI32.dll
```

No dynamic MSVC/UCRT library matched upstream's rejection pattern.

## 4. Run the upstream native tests and upstream Native-AOT sample

```powershell
$env:CSWEBUI_NATIVE_LIBRARY = "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\webui-native-install\bin\webui-2.dll"

dotnet test CsWebUi.sln --configuration Release --no-build --logger "console;verbosity=normal"

dotnet publish samples/CsWebUi.NativeAotSmoke/CsWebUi.NativeAotSmoke.csproj `
  --configuration Release --runtime win-x64 --self-contained true `
  --output artifacts/native-aot-win-x64 `
  -p:PublishTrimmed=true -p:TrimMode=full `
  -p:EnableTrimAnalyzer=true -p:EnableAotAnalyzer=true

& .\artifacts\native-aot-win-x64\CsWebUi.NativeAotSmoke.exe
```

Selected output:

```text
Total tests: 22
     Passed: 22
    Skipped: 0

Generating native code
publish exit 0
WebUI allocated port 16058.
run exit 0
```

The upstream sample output contained only the native executable, PDBs, and XML documentation; it did not carry managed assemblies or a framework runtime payload.

## 5. Publish and run the high-level browser callback probe

Copy the preserved [probe project](probe/README.md) to a temporary directory or build it in place. Point `UpstreamRepositoryRoot` at the immutable wrapper checkout:

```powershell
dotnet publish protocol/mvvm/feasibility/cs-webui-native-aot/probe/WebUIToolkit.MVVM.UpstreamAotProbe.csproj `
  --configuration Release --runtime win-x64 --self-contained true `
  --output "$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\high-level-roundtrip\publish" `
  -p:UpstreamRepositoryRoot="$env:TEMP\webuitoolkit-cs-webui-aot-20260722-main\cs-webui"
```

Selected output:

```text
CsWebUi.Native -> ...\CsWebUi.Native.dll
CsWebUi -> ...\CsWebUi.dll
WebUIToolkit.MVVM.UpstreamAotProbe -> ...\win-x64\WebUIToolkit.MVVM.UpstreamAotProbe.dll
Generating native code
publish exit 0
```

The executable was started hidden with `CSWEBUI_NATIVE_LIBRARY` set to the locally built DLL. After its `READY http://localhost:<ephemeral-port>` line, Edge was started hidden and kept alive:

```powershell
Start-Process `
  -FilePath 'C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe' `
  -ArgumentList @(
    '--headless=new',
    '--disable-gpu',
    '--no-first-run',
    '--no-default-browser-check',
    '--user-data-dir=<unique-temporary-profile>',
    '--remote-debugging-port=0',
    'http://localhost:<ephemeral-port>') `
  -WindowStyle Hidden
```

Final native-process output:

```text
READY http://localhost:17743
CALLBACK argument=native-aot client=0
PASS browser-to-managed-to-browser callback
```

The probe exited `0`. Only Edge processes whose command line contained the unique temporary profile path were then stopped.

## 6. Failed browser-driver attempts

Two attempts substituted Edge `--dump-dom` for the persistent headless process. Both served the page but printed the pre-callback `pending` body, the probe timed out, and the probe exited `3`. No managed callback line appeared. This failure is preserved because a future automated gate must keep the browser alive through WebUI WebSocket authentication; a successful HTTP page load alone is insufficient.
