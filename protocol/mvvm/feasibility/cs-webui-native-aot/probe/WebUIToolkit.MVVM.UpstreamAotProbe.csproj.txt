<!-- Copy this transcript to WebUIToolkit.MVVM.UpstreamAotProbe.csproj before reproduction. -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>WebUIToolkit.MVVM.UpstreamAotProbe</AssemblyName>
    <RootNamespace>WebUIToolkit.MVVM.UpstreamAotProbe</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PublishAot>true</PublishAot>
    <PublishTrimmed>true</PublishTrimmed>
    <TrimMode>full</TrimMode>
    <EnableTrimAnalyzer>true</EnableTrimAnalyzer>
    <EnableAotAnalyzer>true</EnableAotAnalyzer>
    <IsAotCompatible>true</IsAotCompatible>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <ItemGroup Condition="'$(UpstreamRepositoryRoot)' != ''">
    <ProjectReference Include="$(UpstreamRepositoryRoot)\src\CsWebUi\CsWebUi.csproj" />
  </ItemGroup>

  <Target Name="RequireUpstreamRepositoryRoot" BeforeTargets="PrepareForBuild" Condition="'$(UpstreamRepositoryRoot)' == ''">
    <Error Text="Set UpstreamRepositoryRoot to an absolute checkout of the pinned external cs-webui commit." />
  </Target>
</Project>
