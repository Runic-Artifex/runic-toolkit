# Web workspace

`@runic-artifex/application-bridge` is the one framework-neutral web package.
Effect owns schema validation, the application service, event stream, scopes,
mock layers, and fault injection. React, Vue, Svelte, Angular, and future UI
frameworks consume its controller directly and do not own protocol state.
