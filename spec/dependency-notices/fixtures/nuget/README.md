# NuGet inventory fixtures

These fixtures are synthetic, offline global-packages layouts. `valid` covers direct,
transitive, and development-only packages, exact locked versions, package hashes,
SPDX expression metadata, and local license evidence. `hostile` contains intentionally
unsafe or inconsistent graph data. No fixture requires restore or network access.
